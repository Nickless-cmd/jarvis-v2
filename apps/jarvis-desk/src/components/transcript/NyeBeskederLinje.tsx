/** Skillelinjen over den første besked man ikke har set (Claude Desktop §10). */
export function NyeBeskederLinje() {
  return (
    <div role="separator" aria-label="Nye beskeder" className="nye-beskeder" data-testid="nye-beskeder">
      <span>Nye beskeder</span>
    </div>
  )
}
