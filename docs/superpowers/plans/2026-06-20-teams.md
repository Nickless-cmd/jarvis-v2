# Teams Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Indbyggede delte team-samtaler + delt team-git-workspace i Jarvis V2, så Jarvis bliver multi-user-laget vi i dag låner Discord til.

**Architecture:** Tre nye SQLite-tabeller (`teams`, `team_members`, `team_invites`) + `chat_sessions.team_id`. Sessions-adgang udvides med ÉN regel (A: du postede ELLER B: du er team-medlem) der lader private sessioner være urørte (#154-grænsen). Team-workspace er et git-repo i `~/.jarvis-v2/teams/<id>/` med auto-commit. Jarvis er selv et team-medlem med rollen `'jarvis'`. Native tools gør det conversational.

**Tech Stack:** Python 3.11 (conda `ai`) + SQLite backend; React/Vitest desktop (apps/jarvis-desk); React Native/Jest mobil (worktree).

**Spec:** `docs/superpowers/specs/2026-06-20-teams-design.md`

**Decomposition:** Bygges i 3 faser med check-in mellem hver (spec §9). **Denne plan detaljerer Fase 1 fuldt ud.** Fase 2 og 3 er outlinet til sidst og får hver sin detaljerede plan EFTER Fase 1-check-in (undgår spekulativ UI/tool-plan før datamodellen står og er verificeret live).

**Miljø-noter for implementøren:**
- Python: `source /opt/conda/etc/profile.d/conda.sh && conda activate ai` (conda i /opt/conda, IKKE ~/anaconda3). Kør tests: `python -m pytest tests/...`.
- DB lever i `~/.jarvis-v2/...`; `core/runtime/db.py` har `connect()` (context manager) + `init_db()` der kalder en række `_ensure_*(conn)`-funktioner idempotent ved opstart.
- Deploy backend efter en fase: `git push origin main` → `ssh 10.0.0.39 'git -C /media/projects/jarvis-v2 fetch origin -q && git -C /media/projects/jarvis-v2 merge --ff-only origin/main -q && sudo systemctl restart jarvis-api jarvis-runtime'`.
- Verificér ALTID Jarvis-relevante ting med fuld suite før "done".

---

## Fil-struktur (Fase 1)

| Fil | Ansvar | Create/Modify |
|-----|--------|---------------|
| `core/runtime/db.py` | `_ensure_teams_tables()` + `_ensure_chat_session_team_column()`, kaldt fra `init_db()` | Modify (~L204-214 mønster) |
| `core/services/teams.py` | **NY** data-lag: team-CRUD, medlemskab, rolle-opslag, scope-helper | Create |
| `core/runtime/workspace_paths.py` | `team_dir(team_id)` ved siden af `workspace_dir()` | Modify (efter L40) |
| `core/services/chat_sessions.py` | Scoping-regel B i `list_chat_sessions` (L44-91) | Modify |
| `core/tools/session_search.py` | Scoping-regel B i `_user_scope_clause` (L86) | Modify |
| `core/services/cross_user_share_guard.py` | Team-session-bevidsthed i `check_outbound` (L22) | Modify |
| `tests/test_teams.py` | **NY** alle Fase-1-tests | Create |

**Designprincip:** ALT team-data-lag-logik bor i `core/services/teams.py` (én fil, ét ansvar). db.py kender kun schemaet. chat_sessions/session_search kalder en delt helper `teams.team_scope_sql(user_id)` så scoping-reglen B kun skrives ÉT sted.

---

## Task 1: Teams-tabeller + migration

**Files:**
- Modify: `core/runtime/db.py` (tilføj to `_ensure_*`-funktioner + kald dem i `init_db()`)
- Test: `tests/test_teams.py`

- [ ] **Step 1: Skriv den fejlende test**

```python
# tests/test_teams.py
import sqlite3
import core.runtime.db as db


def test_teams_tables_exist_after_init(tmp_path, monkeypatch):
    dbfile = tmp_path / "t.db"
    monkeypatch.setattr(db, "_db_path", lambda: str(dbfile))  # se note nedenfor
    db.init_db()
    with db.connect() as conn:
        names = {r[0] for r in conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table'").fetchall()}
    assert {"teams", "team_members", "team_invites"} <= names
    # chat_sessions har fået team_id-kolonnen
    cols = {r[1] for r in conn.execute("PRAGMA table_info(chat_sessions)").fetchall()}
    assert "team_id" in cols
```

> Note: tjek hvordan db.py finder sti (`_db_path`/`DB_PATH`). Hvis det er en modul-konstant, monkeypatch den i stedet. Brug det mønster eksisterende db-tests bruger (`grep -n "def test" tests/test_*.py | grep -i db`).

- [ ] **Step 2: Kør testen og se den fejle**

Run: `python -m pytest tests/test_teams.py::test_teams_tables_exist_after_init -v`
Expected: FAIL (team-tabeller findes ikke).

- [ ] **Step 3: Tilføj migrations i db.py**

Følg det eksisterende `_ensure_chat_session_workspace_columns`-mønster (L204-214). Tilføj:

```python
def _ensure_teams_tables(conn: sqlite3.Connection) -> None:
    conn.execute("""
        CREATE TABLE IF NOT EXISTS teams (
            team_id        TEXT PRIMARY KEY,
            name           TEXT NOT NULL,
            owner_user_id  TEXT NOT NULL,
            created_at     TEXT NOT NULL,
            workspace_path TEXT NOT NULL
        )""")
    conn.execute("""
        CREATE TABLE IF NOT EXISTS team_members (
            team_id    TEXT NOT NULL,
            user_id    TEXT NOT NULL,
            team_role  TEXT NOT NULL,
            joined_at  TEXT NOT NULL,
            PRIMARY KEY (team_id, user_id)
        )""")
    conn.execute("""
        CREATE TABLE IF NOT EXISTS team_invites (
            token         TEXT PRIMARY KEY,
            team_id       TEXT NOT NULL,
            invited_email TEXT NOT NULL DEFAULT '',
            invited_by    TEXT NOT NULL,
            status        TEXT NOT NULL,
            created_at    TEXT NOT NULL,
            expires_at    TEXT NOT NULL
        )""")


def _ensure_chat_session_team_column(conn: sqlite3.Connection) -> None:
    cols = {r[1] for r in conn.execute("PRAGMA table_info(chat_sessions)").fetchall()}
    if "team_id" not in cols:
        conn.execute("ALTER TABLE chat_sessions ADD COLUMN team_id TEXT")
```

I `init_db()`, efter de øvrige `_ensure_*`-kald, tilføj:
```python
    _ensure_teams_tables(conn)
    _ensure_chat_session_team_column(conn)
```

- [ ] **Step 4: Kør testen og se den passere**

Run: `python -m pytest tests/test_teams.py::test_teams_tables_exist_after_init -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add core/runtime/db.py tests/test_teams.py
git commit -m "feat(teams): SQLite-tabeller + chat_sessions.team_id-kolonne"
```

---

## Task 2: Team data-lag — opret + medlemskab + rolle-opslag

**Files:**
- Create: `core/services/teams.py`
- Test: `tests/test_teams.py`

- [ ] **Step 1: Skriv de fejlende tests**

```python
# tilføj til tests/test_teams.py
import core.services.teams as teams


def _fresh_db(tmp_path, monkeypatch):
    dbfile = tmp_path / "t.db"
    monkeypatch.setattr(db, "_db_path", lambda: str(dbfile))
    db.init_db()


def test_create_team_makes_owner_and_jarvis_members(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    monkeypatch.setattr(teams, "_now_iso", lambda: "2026-06-20T00:00:00Z")
    monkeypatch.setattr(teams, "_new_id", lambda: "team-abc")
    t = teams.create_team("Engineering", owner_user_id="bjorn")
    assert t["team_id"] == "team-abc"
    assert teams.is_member("team-abc", "bjorn") is True
    assert teams.member_role("team-abc", "bjorn") == "owner"
    # Jarvis tilføjes automatisk som deltager, ikke admin
    assert teams.member_role("team-abc", "jarvis") == "jarvis"


def test_list_teams_for_user(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    teams.create_team("A", owner_user_id="bjorn")
    teams.create_team("B", owner_user_id="mikkel")
    mine = {t["name"] for t in teams.list_teams_for_user("bjorn")}
    assert mine == {"A"}  # ikke Mikkels team


def test_add_member_default_editor(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    t = teams.create_team("A", owner_user_id="bjorn")
    teams.add_member(t["team_id"], "mikkel")  # default editor
    assert teams.member_role(t["team_id"], "mikkel") == "editor"
    names = {m["user_id"] for m in teams.list_members(t["team_id"])}
    assert names == {"bjorn", "jarvis", "mikkel"}
```

- [ ] **Step 2: Kør og se dem fejle**

Run: `python -m pytest tests/test_teams.py -k "create_team or list_teams or add_member" -v`
Expected: FAIL (modul/funktioner findes ikke).

- [ ] **Step 3: Implementér `core/services/teams.py`**

```python
"""Team data-lag: CRUD, medlemskab, rolle-opslag, scope-helper.

Eneste sted team-tabellerne tilgås. Holder scoping-reglen (regel B) ÉT sted så
chat_sessions + session_search deler den. 'jarvis'-rollen = deltager, ikke admin.
"""
from __future__ import annotations

import time
import uuid

from core.runtime.db import connect
from core.runtime.workspace_paths import team_dir

JARVIS_USER_ID = "jarvis"


def _now_iso() -> str:  # injicerbart i tests
    return time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime())


def _new_id() -> str:  # injicerbart i tests
    return f"team-{uuid.uuid4().hex[:12]}"


def create_team(name: str, *, owner_user_id: str) -> dict:
    """Opret team + git-workspace; opretteren bliver owner, Jarvis bliver deltager."""
    tid = _new_id()
    ws = str(team_dir(tid))  # opretter + git-init'er mappen (Task 3)
    now = _now_iso()
    with connect() as conn:
        conn.execute(
            "INSERT INTO teams (team_id, name, owner_user_id, created_at, workspace_path) "
            "VALUES (?, ?, ?, ?, ?)",
            (tid, name.strip(), owner_user_id, now, ws),
        )
        conn.execute(
            "INSERT INTO team_members (team_id, user_id, team_role, joined_at) VALUES (?, ?, 'owner', ?)",
            (tid, owner_user_id, now),
        )
        conn.execute(
            "INSERT INTO team_members (team_id, user_id, team_role, joined_at) VALUES (?, ?, 'jarvis', ?)",
            (tid, JARVIS_USER_ID, now),
        )
    return {"team_id": tid, "name": name.strip(), "owner_user_id": owner_user_id,
            "created_at": now, "workspace_path": ws}


def add_member(team_id: str, user_id: str, team_role: str = "editor") -> None:
    with connect() as conn:
        conn.execute(
            "INSERT OR REPLACE INTO team_members (team_id, user_id, team_role, joined_at) "
            "VALUES (?, ?, ?, ?)",
            (team_id, user_id, team_role, _now_iso()),
        )


def member_role(team_id: str, user_id: str) -> str | None:
    with connect() as conn:
        row = conn.execute(
            "SELECT team_role FROM team_members WHERE team_id = ? AND user_id = ?",
            (team_id, user_id),
        ).fetchone()
    return row[0] if row else None


def is_member(team_id: str, user_id: str) -> bool:
    return member_role(team_id, user_id) is not None


def list_members(team_id: str) -> list[dict]:
    with connect() as conn:
        rows = conn.execute(
            "SELECT user_id, team_role, joined_at FROM team_members WHERE team_id = ? ORDER BY joined_at",
            (team_id,),
        ).fetchall()
    return [{"user_id": r[0], "team_role": r[1], "joined_at": r[2]} for r in rows]


def list_teams_for_user(user_id: str) -> list[dict]:
    with connect() as conn:
        rows = conn.execute(
            "SELECT t.team_id, t.name, t.owner_user_id, t.created_at, t.workspace_path "
            "FROM teams t JOIN team_members m ON m.team_id = t.team_id "
            "WHERE m.user_id = ? ORDER BY t.created_at DESC",
            (user_id,),
        ).fetchall()
    return [{"team_id": r[0], "name": r[1], "owner_user_id": r[2],
             "created_at": r[3], "workspace_path": r[4]} for r in rows]


def get_team(team_id: str) -> dict | None:
    with connect() as conn:
        r = conn.execute(
            "SELECT team_id, name, owner_user_id, created_at, workspace_path FROM teams WHERE team_id = ?",
            (team_id,),
        ).fetchone()
    if not r:
        return None
    return {"team_id": r[0], "name": r[1], "owner_user_id": r[2],
            "created_at": r[3], "workspace_path": r[4]}
```

- [ ] **Step 4: Kør og se dem passere**

Run: `python -m pytest tests/test_teams.py -k "create_team or list_teams or add_member" -v`
Expected: PASS. (Kræver `team_dir` fra Task 3 — implementér Task 3 først hvis import fejler, eller monkeypatch `teams.team_dir` i testen midlertidigt. ANBEFALING: lav Task 3 før Task 2's Step 4.)

- [ ] **Step 5: Commit**

```bash
git add core/services/teams.py tests/test_teams.py
git commit -m "feat(teams): data-lag — create_team + medlemskab + rolle-opslag"
```

---

## Task 3: `team_dir()` + git-init af team-workspace

**Files:**
- Modify: `core/runtime/workspace_paths.py` (efter `workspace_dir`, ~L40)
- Test: `tests/test_teams.py`

- [ ] **Step 1: Skriv den fejlende test**

```python
def test_team_dir_creates_git_repo(tmp_path, monkeypatch):
    import core.runtime.workspace_paths as wp
    monkeypatch.setattr(wp, "_root", lambda: tmp_path)  # se note: find rod-helper
    d = wp.team_dir("team-xyz")
    assert d.exists()
    assert (d.parent / ".git").exists() or (d / ".git").exists()  # git-init'et
    assert "teams/team-xyz" in str(d)
```

> Note: tjek hvordan `workspace_dir` finder roden (`grep -n "_root\|jarvis-v2\|home" core/runtime/workspace_paths.py`) og monkeypatch det rette.

- [ ] **Step 2: Kør og se den fejle**

Run: `python -m pytest tests/test_teams.py::test_team_dir_creates_git_repo -v`
Expected: FAIL (`team_dir` findes ikke).

- [ ] **Step 3: Implementér `team_dir`**

Spejl `workspace_dir`'s rod-opslag. Lægger i `<root>/teams/<team_id>/workspace/` og git-init'er `<root>/teams/<team_id>/` (repo-rod over workspace, så .git ikke forurener arbejdsfiler):

```python
import subprocess

def team_dir(team_id: str) -> Path:
    """Delt team-workspace som git-repo. Opretter + git-init'er ved første kald."""
    base = _root() / "teams" / team_id          # brug samme rod-helper som workspace_dir
    ws = base / "workspace"
    ws.mkdir(parents=True, exist_ok=True)
    if not (base / ".git").exists():
        try:
            subprocess.run(["git", "init", "-q"], cwd=str(base), check=False)
            subprocess.run(["git", "config", "user.email", "teams@jarvis"], cwd=str(base), check=False)
            subprocess.run(["git", "config", "user.name", "Jarvis Teams"], cwd=str(base), check=False)
        except Exception:
            pass  # git mangler → workspace virker stadig, bare uden rollback
    return ws
```

- [ ] **Step 4: Kør og se den passere**

Run: `python -m pytest tests/test_teams.py::test_team_dir_creates_git_repo -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add core/runtime/workspace_paths.py tests/test_teams.py
git commit -m "feat(teams): team_dir() — git-init'et delt workspace pr. team"
```

---

## Task 4: Scoping-regel B (A privat ELLER B team-medlem)

**Files:**
- Modify: `core/services/teams.py` (tilføj `team_scope_sql()`)
- Modify: `core/services/chat_sessions.py` (`list_chat_sessions`, L44-91 WHERE-klausul)
- Modify: `core/tools/session_search.py` (`_user_scope_clause`, L86)
- Test: `tests/test_teams.py`

- [ ] **Step 1: Skriv de fejlende tests**

```python
import core.services.chat_sessions as cs


def _post(session_id, user_id, team_id=None):
    """Hjælper: opret session (+ team_id) og en besked stemplet med user_id."""
    with db.connect() as conn:
        conn.execute("INSERT OR IGNORE INTO chat_sessions (session_id, title, created_at, updated_at, team_id) "
                     "VALUES (?, ?, ?, ?, ?)", (session_id, session_id, "t", "t", team_id))
        conn.execute("INSERT INTO chat_messages (message_id, session_id, role, content, user_id) "
                     "VALUES (?, ?, 'user', 'hi', ?)", (f"m-{session_id}-{user_id}", session_id, user_id))


def test_private_session_only_visible_to_author(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    _post("priv1", "bjorn")
    assert {s["session_id"] for s in cs.list_chat_sessions(user_id="bjorn")} == {"priv1"}
    assert cs.list_chat_sessions(user_id="mikkel") == []  # #154 urørt


def test_team_session_visible_to_all_members(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    t = teams.create_team("Eng", owner_user_id="bjorn")
    teams.add_member(t["team_id"], "mikkel")
    _post("team-s1", "bjorn", team_id=t["team_id"])  # kun bjorn har postet
    # Mikkel har IKKE postet, men er medlem → skal SE den (regel B)
    assert "team-s1" in {s["session_id"] for s in cs.list_chat_sessions(user_id="mikkel")}


def test_non_member_cannot_see_team_session(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    t = teams.create_team("Eng", owner_user_id="bjorn")
    _post("team-s2", "bjorn", team_id=t["team_id"])
    assert cs.list_chat_sessions(user_id="fremmed") == []  # ikke-medlem afvist
```

- [ ] **Step 2: Kør og se dem fejle**

Run: `python -m pytest tests/test_teams.py -k "private_session or team_session_visible or non_member" -v`
Expected: `test_team_session_visible_to_all_members` FEJLER (Mikkel ser den ikke endnu).

- [ ] **Step 3: Tilføj `team_scope_sql` i teams.py + brug den begge steder**

```python
# core/services/teams.py
def team_scope_sql(session_alias: str = "s") -> str:
    """SQL-fragment: '... ELLER sessionen er en team-session jeg er medlem af'.
    Bruger placeholder ? for user_id. Returnerer fragmentet UDEN ledende OR."""
    return (
        f"({session_alias}.team_id IS NOT NULL AND EXISTS ("
        f"SELECT 1 FROM team_members tmx WHERE tmx.team_id = {session_alias}.team_id "
        f"AND tmx.user_id = ?))"
    )
```

I `chat_sessions.list_chat_sessions` (L44-91): erstat WHERE-klausulen så den bliver regel A ELLER B, og tilføj en EKSTRA `uid`-parameter:

```python
            rows = conn.execute(
                f"""
                SELECT ... (uændret SELECT) ...
                FROM chat_sessions s
                WHERE (
                    EXISTS (SELECT 1 FROM chat_messages mu
                            WHERE mu.session_id = s.session_id AND mu.user_id = ?)
                    OR {teams.team_scope_sql('s')}
                )
                ORDER BY s.updated_at DESC, s.id DESC
                """,
                (uid, uid),     # <-- to params nu: regel A + regel B
            ).fetchall()
```
(Husk `import core.services.teams as teams` øverst i chat_sessions.py.)

I `session_search._user_scope_clause` (L86): tilføj regel B til den returnerede clause + params:

```python
def _user_scope_clause(user_id: str) -> tuple[str, list[Any]]:
    import core.services.teams as teams
    clause = (
        "AND (EXISTS (SELECT 1 FROM chat_messages mu "
        "WHERE mu.session_id = m.session_id AND COALESCE(mu.user_id, '') = ?) "
        f"OR {teams.team_scope_sql('m')})"   # 'm' = sessions-alias i denne query (verificér aliaset!)
    )
    return clause, [user_id, user_id]
```
> VIGTIGT: verificér hvilket alias sessions-tabellen har i session_search-queryen (L110/L184) og send det til `team_scope_sql()`. Hvis aliaset ikke er `m` for chat_sessions, ret det.

- [ ] **Step 4: Kør og se dem passere**

Run: `python -m pytest tests/test_teams.py -k "private_session or team_session_visible or non_member" -v`
Expected: PASS (alle tre). Kør også de eksisterende scope-tests så #154 ikke regredierer:
`python -m pytest tests/test_device_presence.py tests/ -k "scope or session" -v`

- [ ] **Step 5: Commit**

```bash
git add core/services/teams.py core/services/chat_sessions.py core/tools/session_search.py tests/test_teams.py
git commit -m "feat(teams): scoping-regel B — team-medlemmer ser delte sessioner (privat urørt)"
```

---

## Task 5: cross_user_share_guard — tillad cross-user inde i team-session

**Files:**
- Modify: `core/services/cross_user_share_guard.py` (`check_outbound`, L22)
- Test: `tests/test_teams.py`

- [ ] **Step 1: Skriv den fejlende test**

```python
import core.services.cross_user_share_guard as guard


def test_guard_allows_cross_user_inside_team_session(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    t = teams.create_team("Eng", owner_user_id="bjorn")
    teams.add_member(t["team_id"], "mikkel")
    _post("team-s3", "bjorn", team_id=t["team_id"])
    # En besked der refererer Mikkel i en team-session må IKKE flagges
    res = guard.check_outbound("Mikkel spurgte om X", current_user_id="bjorn", session_id="team-s3")
    assert res.get("blocked") in (False, None)
```

> Læs `check_outbound`-signaturen først (`sed -n '22,58p' core/services/cross_user_share_guard.py`) — den tager måske ikke `session_id` i dag. Hvis ikke: tilføj en valgfri `session_id`-param og lav et tidligt return "ikke blokeret" når sessionen er en team-session brugeren er medlem af.

- [ ] **Step 2: Kør og se den fejle**

Run: `python -m pytest tests/test_teams.py::test_guard_allows_cross_user_inside_team_session -v`
Expected: FAIL.

- [ ] **Step 3: Tilføj team-session-bevidsthed**

I `check_outbound`, tidligt (efter signatur-udvidelse med `session_id: str = ""`):

```python
    if session_id:
        try:
            import core.services.teams as teams
            from core.runtime.db import connect
            with connect() as conn:
                row = conn.execute(
                    "SELECT team_id FROM chat_sessions WHERE session_id = ?", (session_id,)
                ).fetchone()
            team_id = row[0] if row and row[0] else None
            if team_id and teams.is_member(team_id, current_user_id):
                return {"blocked": False, "reason": "team_session_shared_context"}
        except Exception:
            pass  # fail-open til eksisterende guard-logik
```
Find dernæst alle call-sites af `check_outbound` (`grep -rn "check_outbound" core/`) og tråd `session_id` igennem hvor det er tilgængeligt (mindst den synlige visible-run-sti). Hvis en call-site ikke har session_id, lad den kalde uden — default `""` bevarer eksisterende adfærd.

- [ ] **Step 4: Kør og se den passere**

Run: `python -m pytest tests/test_teams.py::test_guard_allows_cross_user_inside_team_session -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add core/services/cross_user_share_guard.py tests/test_teams.py
git commit -m "feat(teams): cross_user_share_guard tillader cross-user i team-sessioner"
```

---

## Task 6: Serverside rolle-håndhævelse

**Files:**
- Modify: `core/services/teams.py` (tilføj `require_role` + `can_admin`)
- Test: `tests/test_teams.py`

- [ ] **Step 1: Skriv de fejlende tests**

```python
def test_only_owner_can_admin(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    t = teams.create_team("Eng", owner_user_id="bjorn")
    teams.add_member(t["team_id"], "mikkel")  # editor
    assert teams.can_admin(t["team_id"], "bjorn") is True
    assert teams.can_admin(t["team_id"], "mikkel") is False   # editor ikke admin
    assert teams.can_admin(t["team_id"], "jarvis") is False   # jarvis ikke admin


def test_kick_requires_admin(tmp_path, monkeypatch):
    _fresh_db(tmp_path, monkeypatch)
    t = teams.create_team("Eng", owner_user_id="bjorn")
    teams.add_member(t["team_id"], "mikkel")
    with pytest.raises(PermissionError):
        teams.remove_member(t["team_id"], "mikkel", acting_user_id="mikkel")  # ikke-admin
    teams.remove_member(t["team_id"], "mikkel", acting_user_id="bjorn")       # admin OK
    assert teams.is_member(t["team_id"], "mikkel") is False
```
(Tilføj `import pytest` øverst i testfilen hvis ikke der.)

- [ ] **Step 2: Kør og se dem fejle**

Run: `python -m pytest tests/test_teams.py -k "only_owner_can_admin or kick_requires_admin" -v`
Expected: FAIL.

- [ ] **Step 3: Implementér rolle-gates i teams.py**

```python
def can_admin(team_id: str, user_id: str) -> bool:
    return member_role(team_id, user_id) == "owner"


def remove_member(team_id: str, user_id: str, *, acting_user_id: str) -> None:
    if not can_admin(team_id, acting_user_id):
        raise PermissionError("kun team-owner kan fjerne medlemmer")
    if user_id == JARVIS_USER_ID:
        raise PermissionError("Jarvis kan ikke fjernes fra et team")
    with connect() as conn:
        conn.execute("DELETE FROM team_members WHERE team_id = ? AND user_id = ?", (team_id, user_id))
```

- [ ] **Step 4: Kør og se dem passere**

Run: `python -m pytest tests/test_teams.py -k "only_owner_can_admin or kick_requires_admin" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add core/services/teams.py tests/test_teams.py
git commit -m "feat(teams): serverside rolle-håndhævelse — can_admin + remove_member"
```

---

## Fase 1 — afsluttende verifikation + deploy

- [ ] **Hele teams-suiten grøn:** `python -m pytest tests/test_teams.py -v`
- [ ] **Ingen regression i scoping/sessions:** `python -m pytest tests/ -k "session or scope or presence or memory" -q`
- [ ] **Syntaks:** `python -m compileall -q core apps/api`
- [ ] **Deploy:** push main → ssh target merge + restart jarvis-api/jarvis-runtime (se miljø-noter).
- [ ] **Live røgtest:** opret et team via `python -c "import core.services.teams as t; print(t.create_team('Smoke', owner_user_id='bjorn'))"` på target; verificér `~/.jarvis-v2/teams/<id>/.git` findes.
- [ ] **CHECK-IN med Bjørn** før Fase 2. Vis: teams-tabeller live, scoping-regel verificeret, git-workspace oprettet.

---

## Fase 2 — Jarvis-tools + invite-flow

**Fase 2a LEVERET 2026-06-20 (commit a99c82ae, live på target):** team_tools.py (create_team/list_teams/invite_to_team) + invite-token-livscyklus (create/get/accept_invite) + fuld registrering (7 steder) + tests. Jarvis kan oprette/liste/invitere via samtale.

**Fase 2b mangler (OUTLINE):**

Følger nøjagtigt geolocation_tools.py-mønsteret (ny fil + 7 registrerings-steder).

- **`core/tools/team_tools.py`** (NY) — `exec_create_team`, `exec_invite_to_team`, `exec_list_teams`. `create_team` læser `current_user_id()`; `invite_to_team` genererer token via en ny `teams.create_invite()` + leverer via (a) `proactive_router.route()` invite-kort hvis modtager online, (b) email-tool.
- **Registrér i `simple_tools.py`**: schema-blok (efter get_exchange_rate) + `_TOOL_HANDLERS` + import. **Plus** `tool_scoping.py`, `permission_engine.py`, `veto_gate.py`, `copilot_tool_pruning.py`, `visible_runs.py`-labels (samme 5 steder som geo-tools).
- **Verificér-trin**: invite-tools markeres som "udadvendt" → går gennem approval-kort (genbrug app_control/approval-mønster) før email/notifikation sendes.
- **`teams.create_invite()` / `accept_invite()` / `expire_invites()`** — token-livscyklus (pending→accepted/expired/revoked) i teams.py + tests.
- **Email-invite**: find/genbrug eksisterende email-afsendelse (`grep -rn "smtp\|sendmail\|jarvis@srvlab" core/`); hvis intet, et lille `core/services/email_tool.py` der læser SMTP-creds fra runtime.json.
- **Permission_engine**: tilføj team-create + team-tools som baseline-evne for alle reelle brugere (ikke owner-gatet).
- **Auto-commit-hook**: når en fil skrives i et team-workspace (via code-mode write/upload) → `subprocess git add+commit` med den handlende bruger som author. Find write-stien (`_exec_write_file` + upload-route) og hook ind når path er under `teams/`.
- **@mention-parser** (`core/services/team_mentions.py`): regex `@navn` → slå op i `team_members`; `@jarvis` → `should_jarvis_respond`-flag; `@medlem` → `proactive_router.route()`.
- **`should_jarvis_respond(besked, kontekst)`** (NY, ÉT beslutningspunkt) — v1: "@jarvis nævnt ELLER svar-på-Jarvis". Wires ind i den synlige run-trigger for team-sessioner.
- **Team-aktivitets-notifikation**: ved ny besked i team-session → `proactive_router` til ikke-tilstedeværende medlemmer (presence), debounced.
- Tests: token-livscyklus, @mention-parser, auto-commit-author, should_jarvis_respond-v1, tool-registrering.
- Check-in med Bjørn.

---

## Fase 3 — App-UI (OUTLINE — detaljeres efter Fase 2-check-in)

- **Backend-API**: `apps/api/jarvis_api/routes/teams.py` — REST for app'en (`GET /teams`, `POST /teams`, `POST /teams/{id}/invite`, `POST /invites/{token}/accept`, `GET /teams/{id}/members`, `DELETE /teams/{id}/members/{uid}`), alle scoped til auth'et bruger + serverside rolle-gates.
- **Desktop (apps/jarvis-desk)**: `src/lib/teamsApi.ts` + `src/components/shell/TeamsSection.tsx` (separat foldbar sektion i sidebar, jf. UX §6.1) + team-styrings-panel (opret/inviter/medlemsliste/kick) + accept-kort (genbrug PresenceHost/AppActionCard-mønster) + @mention-autocomplete i Composer. Vitest for sidebar-state, @mention-parser, accept-flow. Bump version + `npm run package:linux` + dpkg.
- **Mobil (worktree)**: Teams-sektion i sessions-liste + team-session-deltagelse + @mention-FCM-notifikation + accept-invite-skærm + opret-via-Jarvis (allerede dækket af tool). Jest + tsc. **Husk versionCode i android/app/build.gradle (IKKE app.json)** + verificér bundle via `strings APK | grep`.
- **Geolocation i team-kontekst (spec §5.6)**: i team-session-headerens medlemsliste vis delt lokation ved siden af presence-prikken — KUN hvis medlemmet selv deler (respektér deres location-toggle ubetinget). Genbruger `device_presence.location_for` + den eksisterende geolocation-feature; scoped til team-medlemmer.
- Cross-device-sync-test: team-session synlig på begge enheder.
- Check-in med Bjørn + on-device-verifikation.

---

## Deferet (byg IKKE i denne plan — skrevet så det ikke glemmes)
🧠 interjection-motor · 📧 email-onboarding af nye brugere · 👁 viewer/read-only-håndhævelse · 🔇 mute · 🔗 Discord-kanal↔team-bro · 🔐 team-repo-kryptering-at-rest.
